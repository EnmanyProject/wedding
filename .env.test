# Test Environment Configuration
NODE_ENV=test
DEV_MODE_SEED_ENABLED=true

# Test Database (separate from development)
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/wedding_app_test

# Storage (use test bucket/path)
STORAGE_ENDPOINT=http://localhost:9000
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin123
STORAGE_BUCKET=wedding-photos-test
STORAGE_REGION=us-east-1
STORAGE_FORCE_PATH_STYLE=true

# JWT (test secret)
JWT_SECRET=test-jwt-secret-key-for-testing
JWT_EXPIRES_IN=1h

# Quiz Configuration (same as dev)
QUIZ_ENTER_COST=1
QUIZ_WRONG_PENALTY=1
TRAIT_ADD_REWARD=1

# Affinity Thresholds (same as dev)
AFFINITY_ALPHA=3
AFFINITY_BETA=1
AFFINITY_T1_THRESHOLD=20
AFFINITY_T2_THRESHOLD=40
AFFINITY_T3_THRESHOLD=60

# Photo Configuration (same as dev)
PHOTO_THUMB_SIZE=256
PHOTO_BLUR1_SIGMA=4
PHOTO_BLUR2_SIGMA=8
PRESIGN_URL_EXPIRES=300

# Ranking (same as dev)
RANKING_TOP_COUNT=5
RANKING_CACHE_TTL=3600