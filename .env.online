# ========================================
# 온라인 DB 전환용 설정 (.env.online)
# ========================================
#
# 사용법:
# 1. 온라인 DB 서비스 가입 (Supabase, Neon, Railway 등)
# 2. DATABASE_URL을 온라인 DB URL로 변경
# 3. 이 파일을 .env로 복사: copy .env.online .env
# 4. 서버 재시작: npm run dev
#
# ========================================

# ===== 온라인 Database =====
# 예시: Supabase
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
#
# 예시: Neon
# DATABASE_URL=postgresql://[user]:[password]@[host].neon.tech/[dbname]?sslmode=require
#
# 예시: Railway
# DATABASE_URL=postgresql://postgres:[password]@[host].railway.app:5432/railway

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/wedding_app  # ← 여기를 온라인 DB URL로 변경

# ===== 온라인 Storage (MinIO 대신 S3 등) =====
# 로컬: MinIO
# 온라인: AWS S3, Cloudflare R2, Supabase Storage 등

STORAGE_ENDPOINT=http://localhost:9000  # ← S3 사용 시 삭제
STORAGE_ACCESS_KEY=minioadmin            # ← 온라인 키로 변경
STORAGE_SECRET_KEY=minioadmin123         # ← 온라인 시크릿으로 변경
STORAGE_BUCKET=wedding-photos
STORAGE_REGION=us-east-1
STORAGE_FORCE_PATH_STYLE=true            # ← S3 사용 시 false

# ===== 온라인 Redis =====
# 로컬: localhost
# 온라인: Upstash, Redis Cloud 등

REDIS_URL=redis://localhost:6379  # ← 온라인 Redis URL로 변경

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production  # ← 프로덕션용으로 변경!
JWT_EXPIRES_IN=7d

# Server
PORT=3002
NODE_ENV=production  # ← 온라인 배포 시 production으로

# Development/Seeding
DEV_MODE_SEED_ENABLED=false  # ← 프로덕션에서는 false!
USE_MOCK_RING_SERVICE=false

# Quiz Configuration
QUIZ_ENTER_COST=1
QUIZ_WRONG_PENALTY=1
TRAIT_ADD_REWARD=1

# Affinity Thresholds
AFFINITY_ALPHA=3
AFFINITY_BETA=1
AFFINITY_T1_THRESHOLD=20
AFFINITY_T2_THRESHOLD=40
AFFINITY_T3_THRESHOLD=60

# Photo Configuration
PHOTO_THUMB_SIZE=256
PHOTO_BLUR1_SIGMA=4
PHOTO_BLUR2_SIGMA=8
PRESIGN_URL_EXPIRES=300

# Ranking
RANKING_TOP_COUNT=5
RANKING_CACHE_TTL=3600

# AI Image Generation (온라인 배포 시 환경 변수로 관리)
# ⚠️ 아래 키들은 실제 값으로 변경하세요!
REPLICATE_API_TOKEN=r8_your_replicate_api_token_here
OPENAI_API_KEY=sk-proj-your_openai_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here
